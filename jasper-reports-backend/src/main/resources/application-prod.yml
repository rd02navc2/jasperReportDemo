spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/report_prod
    username: root
    password: fslIMX535!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

#spring:
#  datasource:
#    # 1. 修改為 PostgreSQL 的 JDBC URL (預設通訊埠為 5432)
#    url: jdbc:postgresql://127.0.0.1:5432/report_prod
#    username: postgres
#    # 請確保 PostgreSQL 中有對應的密碼
#    password: fslIMX535! 
#    # 2. 修改為 PostgreSQL 的驅動類別名稱
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    # 3. 顯示 SQL 並格式化（對應原本的需求）
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#        # 4. 指定 PostgreSQL 的 SQL 方言 (推薦版本 10 以上使用)
#        dialect: org.hibernate.dialect.PostgreSQLDialect 

jwt:
  secret: "dGhpcy1pcy1teS1zZWNyZXQta2V5LXNob3VsZC1iZS12ZXJ5LXN0cm9uZw=="
  expiration: 86400000 # 24 hours in milliseconds

razorpay:
  key:
    id: ${RAZORPAY_KEY_ID:your-razorpay-key-id}
    secret: ${JWT_SECRET:9a8b7c6d5e4f3g2h1i0j9k8l7m6n5o4p3q2r1s0t9u8v7w6x5y4z3a2b1c0d9e8f}

app:
  frontend:
    url: http://localhost:3000